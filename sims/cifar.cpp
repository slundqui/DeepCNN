//A run file that runs the DCN on cifar data

#include "BaseLayer.hpp"

int main(void){
   std::cout << "CIFAR Run TODO!\n";
   //Build network
   //TODO integrate this with PetaVision's parameter files
   //currently, parameters are explicitly defined through setter calls to each object
   //

   //BaseLayer* myLayer = new BaseLayer("testLayer");
   //int status = myLayer->initialize();




   
}
